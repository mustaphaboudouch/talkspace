<nav class="border-b border-gray-200 bg-white">
    <div class="mx-auto flex w-11/12 max-w-[1400px] items-center justify-between py-4">
        <div>
            <a href="{{ path('app_home') }}" class="text-xl font-bold">
                <span class="text-indigo-600">Talk</span>Space.
            </a>
            {% if app.user %}
                <a href="{{ path('app_dashboard') }}">Dashboard</a>
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('app_admin_users_index') }}">
                        Utilisateurs
                    </a>
                    <a href="{{ path('app_admin_support_index') }}">Messages</a>
                {% endif %}
                {% if is_granted('ROLE_DOCTOR') %}
                    <a href="{{ path('app_doctor_service_index') }}">
                        Services
                    </a>
                    <a href="{{ path('app_doctor_settings') }}">Paramètres</a>
                {% endif %}
                <a href="{{ path('app_profile') }}">Profil</a>
                <a href="{{ path('app_logout') }}">Déconnecter</a>
            {% endif %}
        </div>
        <div>
            {% if app.user %}
                <div class="flex items-center gap-3">
                    <img src="{{
                        asset(
                            'uploads/profile/' ~ app.user.profilePicture
                        )
                        }}"
                        class="h-8 w-8 rounded-full object-cover" />
                    <span class="text-sm font-medium">
                        {{ app.user.firstname }} {{ app.user.lastname }}
                    </span>
                </div>
            {% else %}
                <a href="{{ path('app_login') }}"
                    class="mr-2 inline-flex justify-center rounded-md border border-indigo-600 bg-transparent py-2 px-4 text-sm font-medium text-indigo-600 shadow-sm hover:border-indigo-700 hover:text-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    S'identifier
                </a>
                <a href="{{ path('app_register') }}"
                    class="inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    S'inscrire
                </a>
            {% endif %}
        </div>
    </div>
</nav>
