<nav class="border-b border-gray-300 bg-white">
    <div class="mx-auto flex w-11/12 max-w-[1400px] items-center justify-between py-4">
        <a href="{{ path('app_home') }}" class="text-xl font-bold">
            <span class="text-indigo-600">Talk</span>Space.
        </a>
        <div class="flex items-center">
            {% if app.user %}
                <a href="{{ path('app_dashboard') }}"
                    class="mr-8 flex items-center gap-2 text-sm text-indigo-600">
                    <svg xmlns="http://www.w3.org/2000/svg"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke-width="1.5"
                        stroke="currentColor"
                        class="h-5 w-5">
                        <path stroke-linecap="round"
                            stroke-linejoin="round"
                            d="M2.25 12l8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />
                    </svg>
                    <span class="hover:underline">Dashboard</span>
                </a>
                {% if is_granted('ROLE_PATIENT') %}
                    <a href="{{ path('app_doctor_index') }}"
                        class="mr-8 flex items-center gap-2 text-sm text-indigo-600">
                        <svg xmlns="http://www.w3.org/2000/svg"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke-width="1.5"
                            stroke="currentColor"
                            class="h-5 w-5">
                            <path stroke-linecap="round"
                                stroke-linejoin="round"
                                d="M21 21l-5.197-5.197m0 0A7.5 7.5 0 105.196 5.196a7.5 7.5 0 0010.607 10.607z" />
                        </svg>
                        <span class="hover:underline">
                            Trouver un thérapeute
                        </span>
                    </a>
                {% endif %}
            {% else %}
                <a href="{{ path('app_doctor_index') }}"
                    class="mr-8 flex items-center gap-2 text-sm text-indigo-600">
                    <svg xmlns="http://www.w3.org/2000/svg"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke-width="1.5"
                        stroke="currentColor"
                        class="h-5 w-5">
                        <path stroke-linecap="round"
                            stroke-linejoin="round"
                            d="M21 21l-5.197-5.197m0 0A7.5 7.5 0 105.196 5.196a7.5 7.5 0 0010.607 10.607z" />
                    </svg>
                    <span class="hover:underline">Trouver un thérapeute</span>
                </a>
            {% endif %}
            {% if app.user %}
                <div class="flex items-center gap-3">
                    <img src="{{
                        asset(
                            'uploads/profile/' ~ app.user.profilePicture
                        )
                        }}"
                        class="h-8 w-8 rounded-full object-cover" />
                    <span class="text-sm font-medium">
                        {{ app.user.firstname }} {{ app.user.lastname }}
                    </span>
                </div>
            {% else %}
                <a href="{{ path('app_login') }}"
                    class="mr-2 inline-flex justify-center rounded-md border border-indigo-600 bg-transparent py-2 px-4 text-sm font-medium text-indigo-600 shadow-sm hover:border-indigo-700 hover:text-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    S'identifier
                </a>
                <a href="{{ path('app_register') }}"
                    class="inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    S'inscrire
                </a>
            {% endif %}
        </div>
    </div>
</nav>
